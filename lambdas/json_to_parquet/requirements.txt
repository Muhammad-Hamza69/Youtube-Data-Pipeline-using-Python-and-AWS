# Pinned to replace the AWSSDKPandas-Python311 managed layer this function
# previously relied on. VERIFY these match the layer version actually in use
# in your account before first deploy (aws lambda get-layer-version-by-arn),
# then adjust the pins here if needed — the goal is zero behavior change to
# wr.s3.to_parquet(...) / pd.json_normalize(...).
awswrangler==3.9.1
pandas==2.2.2
# awswrangler only requires pyarrow>=8.0.0 (no upper bound), so pip resolves
# whatever the newest pyarrow release is at build time. If that release has no
# prebuilt wheel for this image's Python/platform, pip falls back to a source
# build requiring a C/C++ compiler that this slim Lambda base image doesn't
# have, and the Docker build fails. Pin to a version with known manylinux
# wheels for cp311 to keep the build reproducible.
pyarrow==17.0.0
