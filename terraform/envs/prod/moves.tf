# Empty on purpose. The previous version of this file held `moved` blocks
# from an earlier Lambda-module refactor — those only matter for remapping
# already-applied state, and the state is empty right now (everything was
# torn down before this rebuild), so there's nothing to remap. If a future
# refactor needs `moved` blocks against a live deployment, add them here.
